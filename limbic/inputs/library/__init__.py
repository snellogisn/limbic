"""The sensory-input library — one sense per file.

Every module here defines exactly one :class:`~limbic.inputs.base.Input`
subclass. The registry auto-discovers them, so adding a sense is just adding a
file. Browse these files to see what the arm can perceive.
"""

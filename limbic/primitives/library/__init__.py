"""The motion-primitive library — one primitive per file.

Every module here defines exactly one :class:`~limbic.primitives.base.Primitive`
subclass. The registry auto-discovers them, so adding a primitive is just adding
a file. Browse these files to see what the arm can do; copy one as a template to
author a new skill.
"""
